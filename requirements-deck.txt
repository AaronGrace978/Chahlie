# Chahlie Steam Deck Edition — extra dependencies
# No PyAudio — voice uses system pw-record + espeak-ng (no compiling!)

-r requirements.txt

# Voice (STT uses SpeechRecognition + Linux pw-record; TTS uses espeak-ng)
SpeechRecognition>=3.10.0

# Optional offline speech (download a Vosk model, set CHAHLIE_VOSK_MODEL)
# vosk>=0.3.45

# Optional PyAudio fallback on machines that already have it installed
# PyAudio>=0.2.14
