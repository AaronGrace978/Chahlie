# Chahlie - The Boston Coding Agent
# Official Product of Cursor Boston

ollama>=0.4.0
anthropic>=0.25.0
rich>=13.7.0
textual>=0.50.0
click>=8.1.0
python-dotenv>=1.0.0
requests>=2.31.0

# Optional: persistent vector store for semantic memory.
# If chromadb isn't installed, Chahlie silently falls back to the
# in-process SemanticMemory (no behavior change).
chromadb>=0.5.0 ; python_version >= "3.9"
